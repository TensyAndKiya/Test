package com.clei.leetcode;

/**
 * 电话号码的字母组合
 * 给定一个仅包含数字 2-9 的字符串，返回所有它能表示的字母组合。答案可以按 任意顺序 返回。
 * <p>
 * 给出数字到字母的映射如下（与电话按键相同）。注意 1 不对应任何字母。
 * <p>
 * <p>
 * <p>
 * <p>
 * <p>
 * 示例 1：
 * <p>
 * 输入：digits = "23"
 * 输出：["ad","ae","af","bd","be","bf","cd","ce","cf"]
 * 示例 2：
 * <p>
 * 输入：digits = ""
 * 输出：[]
 * 示例 3：
 * <p>
 * 输入：digits = "2"
 * 输出：["a","b","c"]
 *
 * @author KIyA
 */
public class Algorithm0017 {

    /*private final static Map<Character, String> phoneMap = new HashMap<Character, String>() {{
        put('2', "abc");
        put('3', "def");
        put('4', "ghi");
        put('5', "jkl");
        put('6', "mno");
        put('7', "pqrs");
        put('8', "tuv");
        put('9', "wxyz");
    }};

    public static void main(String[] args) {

        List<String> result = Math.pow(3,)
        List<String> result = letterCombinations()
        letterCombinations("23", 0).forEach(s -> PrintUtil.log(s));
    }

    private static List<String> letterCombinations(String digits, int index) {
        char[] arr = digits.toCharArray();
        int length = arr.length;
        List<String> strList = new ArrayList<>(length);
        StringBuilder sb = new StringBuilder(length);
        for (int i = 0; i < length; i++) {
            for (int j = 0; j <; j++) {

            }
        }
    }*/

}
